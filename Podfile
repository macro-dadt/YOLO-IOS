# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'YOLO-IOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'TensorFlow-experimental', '~> 1.1.1'
  pod 'OpenCV', '~> 3.3.1'
  # Pods for YOLO-IOS

end
